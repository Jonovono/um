#!/usr/bin/env node

var CLI = require('../lib/cli');
var args = process.argv.slice(2);

// console.log(CLI);
// CLI(args);

// CLI();

// new CLI();
new CLI().argv(args);

// CLI().argv(args);
// CLI().argv(args);
// console.log(CLI);